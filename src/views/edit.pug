extends base.pug

block content 
h5 Change Title of video
    form(action="/save-changes", method="POST")
        input(name="title", placeholder="Video Title", value=pageTitle, required)
        input(value="Save", type="submit")